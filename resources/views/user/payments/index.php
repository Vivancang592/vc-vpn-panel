<?php
$pageTitle = 'Giao dịch - ' . ($settings['site_title'] ?? 'VC VPN 2027');
$items = is_array($payments ?? null) ? $payments : [];
$labels = ['pending' => 'Chờ xử lý', 'success' => 'Thành công', 'failed' => 'Thất bại'];
ob_start();
?>
<section class="user-record-page">
	<header class="user-record-header"><div><h1>Giao dịch</h1><p>Theo dõi các khoản nạp tiền, mua gói và gia hạn.</p></div><a class="glass-btn" href="/checkout?type=deposit">Nạp tiền</a></header>
	<?php if (!empty($_SESSION['success']) || !empty($_SESSION['error'])): ?><div class="user-record-alert <?= !empty($_SESSION['error']) ? 'is-error' : '' ?>"><?= htmlspecialchars($_SESSION['error'] ?? $_SESSION['success']) ?></div><?php unset($_SESSION['success'], $_SESSION['error']); endif; ?>
	<?php if ($items): ?><div class="glass-card user-record-table-wrap"><table class="user-record-table"><thead><tr><th>Mã giao dịch</th><th>Loại</th><th>Số tiền</th><th>Trạng thái</th><th>Thời gian</th><th></th></tr></thead><tbody><?php foreach ($items as $item): ?><?php $status = $item['status'] ?? 'pending'; $isDeposit = ($item['type'] ?? '') === 'deposit'; ?><tr><td data-label="Mã giao dịch"><code><?= htmlspecialchars($item['transaction_id'] ?? ('#' . ($item['id'] ?? ''))) ?></code></td><td data-label="Loại"><?= $isDeposit ? 'Nạp tiền' : (!empty($item['subscription_id']) ? 'Gia hạn' : 'Thanh toán') ?></td><td data-label="Số tiền" class="user-record-amount"><?= $formatMoney($item['amount'] ?? 0) ?></td><td data-label="Trạng thái"><span class="user-record-status is-<?= htmlspecialchars($status) ?>"><?= htmlspecialchars($labels[$status] ?? $status) ?></span></td><td data-label="Thời gian"><?= !empty($item['created_at']) ? date('d/m/Y H:i', strtotime($item['created_at'])) : '-' ?></td><td class="user-record-action"><?php if ($status === 'pending'): ?><a href="/payment/checkout?<?= $isDeposit ? 'deposit=' . (int) $item['id'] : (!empty($item['subscription_id']) ? 'renewal=' . (int) $item['subscription_id'] . '&payment=' . (int) $item['id'] : 'order=' . (int) $item['order_id']) ?>">Thanh toán</a><?php endif; ?><?php if ($isDeposit && $status === 'pending'): ?><form method="post" action="/payments/deposit/cancel" data-confirm-submit="Bạn có chắc muốn hủy giao dịch nạp tiền này?"><input type="hidden" name="csrf_token" value="<?= htmlspecialchars($csrf_token ?? '') ?>"><input type="hidden" name="payment_id" value="<?= (int) $item['id'] ?>"><button type="submit" class="user-record-cancel">Hủy</button></form><?php endif; ?></td></tr><?php endforeach; ?></tbody></table></div><?php else: ?><div class="glass-card user-record-empty"><h2>Chưa có giao dịch</h2><p>Các khoản nạp tiền và thanh toán của bạn sẽ xuất hiện tại đây.</p></div><?php endif; ?>
</section>
<?php $content = ob_get_clean(); $showSidebar = true; require_once __DIR__ . '/../../layouts/app.php'; ?>
